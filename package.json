{
  "homepage": "https://estebancaso.github.io/SpaceScope/",
  "name":"spacescope",
  "private":"true",
  "version": "0.0.0",
  "type": "module",

  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint. --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "three": "^0.170.0"
  }
}
